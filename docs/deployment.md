# Deployment

Build images with Docker, push to Docker Hub, then configure the Docker Hub image names in Kubernetes/Helm.

K3d is the local Kubernetes environment for this project. Production AWS deployment is a later phase.
